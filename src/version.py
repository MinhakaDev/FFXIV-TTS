# The running build's version. CI overwrites this line with the released tag before
# PyInstaller runs (see .github/workflows/build.yml); the dev default is what source
# runs and local builds keep, which deliberately silences the update check for them.
__version__ = "0.0.0-dev"
